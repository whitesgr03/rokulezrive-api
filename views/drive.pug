extends layout

block head
	link(rel="stylesheet", href="/stylesheets/drive.css", nonce=cspNonce)
	link(rel="stylesheet", href="/stylesheets/file_list.css", nonce=cspNonce)
	link(rel="stylesheet", href="/stylesheets/shared_list.css", nonce=cspNonce)

block content
	.drive
		h2 My Drive
		if shared
			include shared_list

		if files
			include file_list
