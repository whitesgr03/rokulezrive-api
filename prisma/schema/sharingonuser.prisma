model SharingOnUser {
  member   User @relation(fields: [memberId], references: [pk])
  memberId Int

  sharing Sharing @relation(fields: [shareId], references: [pk])
  shareId Int

  sharedAt DateTime @default(now())

  @@id([shareId, memberId])
}
