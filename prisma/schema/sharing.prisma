model Sharing {
    pk       Int      @id @default(autoincrement())
    id       String
    duration DateTime
    members  User[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
