model Sharing {
  pk     Int     @id @default(autoincrement())
  id     String  @unique @default(uuid())
  anyone Boolean

  members SharingOnUser[]

  file   File @relation(fields: [fileId], references: [pk])
  fileId Int  @unique

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
}
