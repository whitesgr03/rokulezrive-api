model User {
    id       Int       @id @default(autoincrement())
    email    String    @unique
    password String
    folders  Folder[]
    shared   Sharing[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
